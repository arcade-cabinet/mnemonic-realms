/**
 * Markdown Loader — Re-export from utils/
 *
 * Preserved for backwards compatibility. Import from gen/utils/ directly.
 */

export {
  assembleDocRefContext,
  extractSection,
  extractTable,
  parseHeading,
  resolveDocRef,
  resolveDocRefTable,
} from '../utils/index';
